/**
 * Key Term Selection Modal
 *
 * Chunked modal for selecting key terms from a scan.
 * TODO: Implement in Phase 2
 */

import { App, Modal } from "obsidian";

export class KeyTermModal extends Modal {
	constructor(app: App) {
		super(app);
	}

	onOpen(): void {
		// TODO: Implement chunked key term selection
	}

	onClose(): void {
		// Cleanup
	}
}
